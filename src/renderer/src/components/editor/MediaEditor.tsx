/** @module editor/MediaEditor — Top-level NLE editor shell (Phase 1 will implement). */
export default function MediaEditor(): React.JSX.Element {
  return (
    <div className="flex items-center justify-center h-full text-surface-400">
      <p className="text-lg">NLE Editor — coming soon</p>
    </div>
  )
}
